# URL base da API do backend Spring Boot
# Em desenvolvimento o proxy do vite.config.js já redireciona automaticamente
# Só mude se o backend estiver em outro servidor
VITE_API_URL=http://localhost:8080/api